package pages;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class LetCodeEditPage {
	
	protected static final Logger logger = LogManager.getLogger(LetCodeEditPage.class);
	protected WebDriver driver;
	
	public LetCodeEditPage (WebDriver driver) {
		
		this.driver = driver;
		
	}
	
	
	
	

}
